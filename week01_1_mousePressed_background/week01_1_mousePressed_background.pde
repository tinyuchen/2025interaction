// File > Prefrence > 字型放大
// 按下「三角形」執行 Run
// File-Save As 存檔

void setup(){
  size(500, 500);
}
void draw(){
  // 用 mouse 來互動
  if(mousePressed) background(#FF0000);
  else background(#00FF00);
}
